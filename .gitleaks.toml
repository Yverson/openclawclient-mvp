title = "openclawclient-mvp"

[[rules]]
id = "generic-api-key"
description = "Generic API key"
regex = '''(?i)(api[_-]?key|secret|token)\s*[:=]\s*[A-Za-z0-9_\-]{16,}'''

[allowlist]
paths = [
  "deploy/.env.dev",
  ".env.example",
  "deploy/env/.env.example",
  "deploy/secrets/README.md"
]
