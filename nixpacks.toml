# Nixpacks config for Railway / Nixpacks builds
# Force Node 20 (default is 18, required by jsdom, mermaid, marked, etc.)
[variables]
NIXPACKS_NODE_VERSION = "20"

# Use npm install instead of npm ci to avoid lock file sync issues with workspaces
[phases.install]
cmds = ["npm install"]
