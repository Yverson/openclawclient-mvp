// App.tsx - Now using React Router (see router.tsx and main.tsx)
// This file is kept for backwards compatibility but is no longer used

import React from "react"
import "@/styles/globals.css"

const App: React.FC = () => {
  return <div>App - Router is configured in main.tsx</div>
}

export default App
