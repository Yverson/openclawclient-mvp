#!/usr/bin/env bash
set -euo pipefail

# Placeholder auto-run runner (v1.5)
# Règle: rien n’est auto-exécutable sans tag `auto-ok` + `safe`.
# Ce script ne fait rien tant que TaigaSync + mapping ne sont pas branchés.

echo "auto_run_safe: noop (TaigaSync not wired yet)"
